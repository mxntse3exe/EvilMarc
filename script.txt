sudo apt-update
sudo apt-get install apache2

sudo service apache2 start


sudo apt-get install mariadb-server


sudo mysql_secure_installation

(password root)
- Establim password de root --> Stucom1234

(Switch de unix)
- Switch to unix_socket authentication --> No


(Canvi de pass root?)
- Change the root password --> No


(Eliminar usuaris anonims)
Reove anonymus users --> Yes


(Connexio root forma remota)
Disallow root login remotely --> Yes

(Eliminar les test de database)
Remove test database and acces to it --> Yes


(Eliminar privilegis de taules)
Reload privileges tables--> Yes 


sudo mysql

use mysql;

ALTER USER 'root'@'localhost'IDENTIFIED BY 'Stucom1234';

FLUSH PRIVILEGES;

exit

mysql -u root -p (Stucom1234)

sudo apt-get install php

sudo apt-get install php-mysql

sudo service apache2 restart


cd /var/www/html

(Per eliminar el nom de les extension .php, .html)
sudo nano .htaccess 

enganxem:


RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^([^\.]+)$ $1.php [NC,L]
RewriteRule ^([^\.]+)$ $1.html [NC,L]


sudo service apache2 restart




<Directory /var/www/>
                Options Indexes FollowSymLinks
                AllowOverride All
                Require all granted
</Directory>
